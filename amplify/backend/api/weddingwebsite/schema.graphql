type UserSettings @model @auth(rules: [{allow: public}]) {
  id: ID!
  user: String!
  language: String
}

type Restaurant @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String!
  city: String!
}
 